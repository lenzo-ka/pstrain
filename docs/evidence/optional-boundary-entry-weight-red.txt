Optional-boundary multipron entry weighting: behavioral red history
===================================================================

Baseline: feature head 032c5745c1bf71aa7bb2b2c5dc0646b0457f323f
Test overlay: numeric entry-mass assertion in test_bw_state_seq_build.c
Command:

  cmake --build build --target test_bw_state_seq_build -j5
  ctest --test-dir build -R '^bw_state_seq_build$' --output-on-failure

Relevant output:

  FAIL: multipron bypass entry alternatives preserve unit fan-out mass
  FAIL: on mode bypasses boundaries
  0% tests passed, 1 tests failed out of 1

The fixture's first spoken word, `a`, has two pronunciations. The old forward
initializer assigned unit mass to each marked bypass entry, so their combined
mass exceeded the graph convention's unit fan-out mass. This was a behavioral
failure on the original feature head, not a missing-member compile failure.
